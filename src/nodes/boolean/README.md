# Boolean Operations

Since all inputs and outputs are represented as floating-point numbers, an
arbitrary rule for boolean values is required. The chosen rule is `value > 0.5`.
While not perfect, it is simple and works reliably despite floating-point
precision issues.
